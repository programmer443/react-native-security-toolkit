# Test fixtures

`sarif-2.1.0.schema.json` is the official SARIF 2.1.0 JSON Schema, retrieved from
<https://json.schemastore.org/sarif-2.1.0.json> (canonical id
`https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json`) on
2026-08-19. It is committed unmodified so the SARIF reporter is validated against the specification
rather than against our own reading of it, offline and reproducibly.
