<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately insecure fixture. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.fixture.vulnerable">
  <application
    android:allowBackup="true"
    android:debuggable="true"
    android:usesCleartextTraffic="true">
    <activity android:name=".MainActivity" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <service android:name=".SyncService" android:exported="true" />
    <activity android:name=".LinkActivity" android:exported="true" android:permission="com.fixture.LINK">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="https" android:host="fixture.example.test" />
      </intent-filter>
    </activity>
  </application>
</manifest>
